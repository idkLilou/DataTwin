services:
  datatwin:
    build:
      context: ./app
      dockerfile: Dockerfile
    container_name: datatwin
    env_file:
      - ./app/.env
    ports:
      - "8000:8000"
    volumes:
      - datatwin_data:/data

volumes:
  datatwin_data: